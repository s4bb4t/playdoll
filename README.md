# **PlayDoll**

### *“Игра про монстр-хай? У нас есть игра про монстр-хай дома.”*

## 💡 Идея

Игра на Go, где у тебя есть набор “кукол” (dolls).
Ты можешь “поговорить” с любой из них, написав фразу — если фраза совпадает с одной из известных ей реплик, кукла отвечает своим заранее прописанным стилем.

Игрок вводит фразу → программа проверяет, знает ли кукла ответ → отвечает или молчит (“🤷‍♀️ ...я не знаю, что сказать”).

---

# Термины

#### Реализовать - 
    Написать кодом, чтобы работало

#### Сканнер:
    тип `Scanner` из пакета `bufio`. Он может читать из консоли - `os.Stdin`

#### os.Stdin:
    Стандартный инпут - наша консоль или терминал (синонимы). 

#### Фраза, ответ:
    просто строка 

#### Корень проекта:
    папке, где лежит все, что свазанно с программой.
    В нашем случае это папка `playdoll`. 
    Другими словами - самая верхняя или главная папка

#### Неэкспортируемые поля:
    поля, написанные с маленькой буквы,
    чтобы их не было видно за пределами пакета. 

---

## Логика
### 1. Куклы.

Каждая кукла — структура Doll:

```go
type Doll struct {...} // поля нужно написать
```

У каждой куклы должны быть неэкспортируемые имя и фамилия, и заготовленные фразы и ее ответ на них. Достаточно всего двух кукол и по две фразы каждой. Примеры ниже

Общение с куклой должно быть через метод `Doll.Talk(phrases string) string {}`,где Doll - какая то кукла. Метод предстоить реализовать (написать)

### 2. Ввод.

Сначала пользователь (мы) должен запустить нашу программу. 
Затем через **сканнер** мы должны **считать имя** куклы с которой он хочет поговорить.
Сразу после, пользователь вводит свою фразу через тот же сканнер, получает ответ

### 3. Вывод.
Ввод от пользователя и ответ куклы нужно оформить как диалог в книгах:

```
Пользователь окликнул <имя куклы>. Он остановился напротив нее, 
немного помялся, и наконец произнес: "<фраза>".
  
"<ответ>" - сказала <имя и фамилия куклы>.
```

---

## Как писать программу?
1. создать точку входа программы (`main.go`, `func main()`) в корне проекта
2. создать пакет `doll` в одноименной папке в корне проекта
3. создать структуру `Doll` в пакете `doll` с полями:
   - имя (строка)
   - фамилия (строка)
   - фраза - ответ (подумай сама как их сделать?)
   Какие у нас варианты какое поле написать? Отдельное поле на каждый вопрос и ответ? Слайс вопросов и слайс ответов или как-то еще? 

4. написать функцию, которая создает новую куклу из данных ей имени, фамилии, и фраз с ответами
5. написать метод для структуры `Doll` который позволяет общаться с ней.
6. создать в `main` пару кукол
7. создать в `main` сканнер
8. отсканировать пользовательский ввод
9. выдать пользователю ответ
10. выйти из программы 

---

## 🧁 Примеры кукол

### 1. **Клодин Вульф (Clawdeen Wolf)** — стиль: дерзкий, уверенный, немного “fashion queen”.

**Знает фразы:**

| Фраза           | Ответ                                                                   |
| --------------- | ----------------------------------------------------------------------- |
| "привет"        | "Эй, волчица всегда на стиле! Ты как?"                                  |
| "как дела"      | "О, милашка, как всегда — идеально, просто луна сияет для меня!"        |
| "что ты носишь" | "Только мой фирменный фиолетовый и немного когтей. Стиль — это оружие!" |

---

### 2. **Дракулаура (Draculaura)** — стиль: милая, эмоциональная, немного вамп-драматичная.

**Знает фразы:**

| Фраза      | Ответ                                                              |
| ---------- | ------------------------------------------------------------------ |
| "привет"   | "О, хеллооо! Я так рада тебя видеть, просто кровь кипит!"          |
| "как дела" | "Ужасно прекрасно! Пью смузи из клубники и читаю модный журнал 🩸" |
| "любовь"   | "Ой, я вся краснею! Любовь — это как закат, только вечный!"        |

---

### 3. **Фрэнки Штейн (Frankie Stein)** — стиль: немного наивная, любопытная, «на электричестве».

**Знает фразы:**

| Фраза         | Ответ                                                             |
| ------------- | ----------------------------------------------------------------- |
| "привет"      | "Здарова! Надеюсь, ток между нами пробежал ⚡"                     |
| "как дела"    | "Ой, сегодня чуть не перегорела, но всё супер, я подзарядилась!"  |
| "что делаешь" | "Проверяю свои винтики и думаю, как улучшить контакт с людьми 🤖" |

---

# Пример работы программы:
```go
Пользователь окликнул Фрэнки. Он остановился напротив нее, 
немного помялся, и наконец произнес: "привет".
  
"Здарова! Надеюсь, ток между нами пробежал ⚡" - сказала Фрэнки Штейн.
```







# Exapmle

```go

type Person struct {
	Age int
	Height int
	Name string
	Surmane string
	
	Hoobies ???
}

hoobie1 := "walleybool"
hoobie2 := "football"
hoobie3 := "hiking"
hoobie4 := "farting"
hoobie5 := "origami"

```

```go

type Person struct {
    Height1 int
    Height2 int
    Height3 int
    Height4 int
    Height5 int
    Height6 int
    ...
}

type Person struct {
	AgeHeigth map[int]int
}

mapa := map[int]int

```




```go
question1 := "2+2=?"
answ1 := "4"

question2 := "4+2=?"
answ2 := "6"
...

question228 := "sosal?"
answ288 := "da"

type Exam struct {
    QuestionAnswers ???
}

```